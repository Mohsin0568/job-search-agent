logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: debug
spring:
  application:
    name: job-search-agent
  ai:
    openai:
      api-key: ${OPEN_API_KEY}

job-search:
  resume-path: classpath:resume.pdf
